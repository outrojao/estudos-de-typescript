/*
* | MATRIZES |
As matrizes podem ser escritas de duas maneiras.
* Na primeira, você usa o tipo dos elementos seguidos por colchetes ([ ]) para indicar uma matriz desse tipo de elemento:
? let list: number[] = [1, 2, 3];
* A segunda maneira usa um tipo Array genérico, usando a sintaxe Array<type>:
? let list: Array<number> = [1, 2, 3];
! Não há vantagem em usar uma em relação à outra, portanto, cabe a você decidir qual sintaxe usar.
*/

let list: number[] = [1, 2, 3];
let lista: Array<number> = [1, 2, 3];